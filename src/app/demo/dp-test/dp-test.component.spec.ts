import { DpTestComponent } from './dp-test.component';

describe('Component: Test', () => {
  it('should create an instance', () => {
    const component = new DpTestComponent();
    expect(component).toBeTruthy();
  });
});
